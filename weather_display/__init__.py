"""
Weather Display Package.

This package contains the core modules for the Weather Display application,
including configuration, GUI, services (API clients, time), and utilities
(helpers, localization, icon handling).
"""
# This file can remain empty or be used for package-level initializations
# or exposing specific components via `from .module import Class`.
# For now, it primarily marks the directory as a Python package.
